spring:
  application:
    name: arya-api
  profiles:
    active: dev
server:
  port: 8000

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always