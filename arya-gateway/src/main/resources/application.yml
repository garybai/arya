spring:
  application:
    name: arya-gateway
  profiles:
    active: dev
server:
  port: 8002

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always